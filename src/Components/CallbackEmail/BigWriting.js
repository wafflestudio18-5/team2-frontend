import styled from "styled-components"
import Color from "../../Constants/Color"

const BigWriting = styled.h1`
  letter-spacing: -0.04em;
  font-family: "Lora";
  color: ${Color.black};
  margin: 0;
  margin-bottom: 20px;
  font-size: 32px;
  line-height: 36px;
  transform: translateY(-2px);
  font-weight: 500;
`

export default BigWriting
