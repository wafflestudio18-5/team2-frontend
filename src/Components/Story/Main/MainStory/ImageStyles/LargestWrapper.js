import styled from 'styled-components';

const LargestWrapper = styled.figure`
    display: block;
    z-index: 1;
    width: 100%;
    word-break: break-word;
    justify-content: center;
    box-sizing: border-box;
    margin: 0;
`;

export default LargestWrapper;
