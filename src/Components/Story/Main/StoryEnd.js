import styled from 'styled-components';
import Color from '../../../Constants/Color';

const StoryEndWrapper = styled.div`
    margin: 0 auto;
    max-width: 680px;
    min-width: 0px;
    width: 100%;
    background: red;
    justify-content: center;
`;

const StoryEnd = () => {
    return <StoryEndWrapper>StoryEnd</StoryEndWrapper>;
};

export default StoryEnd;
